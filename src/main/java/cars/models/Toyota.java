package cars.models;

import cars.Car;

public class Toyota extends Car {
    public Toyota(int year, int horsePower, double engineCapacity, String model, String color, String transmission) {
        super(year, horsePower, engineCapacity, model, color, transmission);
    }
}
