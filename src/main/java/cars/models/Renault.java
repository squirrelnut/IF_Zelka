package cars.models;

import cars.Car;

public class Renault extends Car {
    public Renault(int year, int horsePower, double engineCapacity, String model, String color, String transmission) {
        super(year, horsePower, engineCapacity, model, color, transmission);
    }
}
