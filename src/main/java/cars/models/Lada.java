package cars.models;

import cars.Car;

public class Lada extends Car {
    public Lada(int year, int horsePower, double engineCapacity, String model, String color, String transmission) {
        super(year, horsePower, engineCapacity, model, color, transmission);
    }
}
